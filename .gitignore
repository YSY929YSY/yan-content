# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# local env files
.env*.local

# typescript
*.tsbuildinfo

app-example

# generated native folders
/ios
/android

# R temporary files
.RData
.Rhistory
*.RData
*.Rhistory

# JMdict/词典数据(本地下载,不入库;见 tools/anchor-jmdict.py 文档)
tools/data/

# 词书导入中间产物(可由 build-*-staging.py 再生)
# 要用 **/staging/* 三个条件都满足才行:
#   · 不能写 staging/  —— 目录整个被排除时,里面的文件再也 negate 不回来
#   · 不能写 staging/* —— 带斜杠的模式**锚定 .gitignore 所在目录**,
#     那样就只管仓库根下的 staging,YanApp/staging 会整个漏出来(含 117MB 的 jmdict)
#   · **/ 才能恢复「任意层级的 staging」这个原意
**/staging/*

# 但词场不是「可再生的中间产物」,它是**手写内容**:
# batch-3/4/5 有生成脚本,batch-1/2 **没有**,丢了就得重写。
# 候选表和合并日志同理,是人挑过的。这几份小(每份 10~70KB),必须进仓库。
# 2026-08-13 提交时才发现它们一直躺在 gitignore 里。
!YanApp/staging/wordfield-batch-*.json
!YanApp/staging/wordfield-candidates*.json
!YanApp/staging/wordfield-merge-log.json

# 外部审核的判读结果同理:它们是**人做过的决定**,不是可再生的中间产物。
# 重审一遍 = 重新花掉一次外部额度 + 负责人的时间。
# 2026-08-30 发现 gpt-verdicts-301.json(301 条判读)一直没进仓库。
!YanApp/staging/*-for-review.md
!YanApp/staging/gpt-verdicts-*.json
!YanApp/staging/wordfield-shortlist-*.json
!YanApp/staging/*-candidates.md
!YanApp/staging/deep-card-audit.md

# Supabase CLI 本地状态(含项目 ref,不该进仓库)
YanApp/supabase/.temp/

# Local YanApp workspace material; never package or stage these from the repo root.
/YanApp_backup_0501/
/resources/
/yan-content/content.json
/yan-content/content.v1.json
/yan-content/README.md
/yan-content/yan_word_story输入模式探讨26.6.2.html
/YanApp/调研/

# Existing local/generated artifacts in the repository root; preserve them, but
# keep them out of status and EAS input.
/.DS_Store
/.expo/
/staging/
/docs/.DS_Store
/docs/.Rhistory
