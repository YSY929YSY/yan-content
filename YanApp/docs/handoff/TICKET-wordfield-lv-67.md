# 工单 · LV 67 条按项目标准重判（判据换了，结论会翻）

> 先读 `AGENTS.md`（第五节 5-1~5-5、第七节诚实要求）。
> **⚠️ 先读 5-5：不要整份读 `App.js`、内容包、`archive/**`。**
> 本轮**不落库**，只重判 + 导出待审清单。

## 本轮决策指标（5-4）

**重判后判定为「该落」的条数。**

不是"改了多少条判据"，是"**能进内容包的多了几条**" —— 那才决定词场能推到多少。

## 这张工单把主线推迟多久

**不推迟，就是主线。** 词场 200 → 最多 265 / 563。

## 为什么要重判：原判据是外部给的，不是项目标准

外部审核用的判据是「**适合作 N5 主例句**」。**那句话是审核委托方随手写的，
不在项目任何标准文件里。**

项目自己的标准：

- `SOUL.md:115`：例句要 **目标词突出、短、真实、中文自然、适合朗读跟读**
- 词卡九标准第 1 条：**真实 —— 旅行者真实会遇到，不是课本造的**

**「超出 N5」不在标准里，「真实」才在。**
按 N5 语法严格过滤，剩下的正好是课本造的句子 —— 直接违反第一条。

已测数据（复算命令见下）：

```
LV 67 条 | 其中 2 条 anchor 已有词场（排除）→ 待处理 65 | 无备选 21
句长：≤12 字 42 条 · 13–16 字 24 条 · 17–20 字 1 条 · >20 字 0 条
```

**42 条本来就短。** 样例：

```
雨は一週間降り続いた。     近くで犬が吠えている。     豚肉は私には合わない。
```

自然的真实日语，只是词汇超纲。**按项目标准该落。**

复算：
```bash
node -e 'const v=require("./staging/gpt-verdicts-301.json");const c=require("./assets/content.fallback.json");const has=new Set((c.wordBank||[]).filter(w=>w&&w.wordField).map(w=>w.id));const L=s=>[...s].length;const lv=v.LV;console.log(lv.length, lv.filter(x=>has.has(x.anchor)).length, lv.filter(x=>x.alt===0).length, JSON.stringify(lv.reduce((b,x)=>{const n=L(x.jp);const k=n<=12?"<=12":n<=16?"13-16":n<=20?"17-20":">20";b[k]=(b[k]||0)+1;return b;},{})))'
```

## 要做的

### 1. 按项目标准重判 65 条（排除已有词场的 2 条）

**判据只有三条，全部来自 `SOUL.md`，不要自己加第四条：**

| 判据 | 怎么判 | 不符 → |
|---|---|---|
| **短** | 句长 ≤ 16 字（分布主峰在 ≤12，13–16 是长尾） | `SWAP` |
| **真实** | 不是课本造的；`SPOKEN` 缩约算**加分**不算扣分 | `SWAP` |
| **目标词突出** | anchor 词是句子的语义重心，不是背景板 | `SWAP` |

**「词汇超出 N5」不是扣分项。** gloss 逐块理解本来就是兜这个的
（当前设备条件覆盖率 96.60%）。

**唯一的例外**：如果 anchor 词本身在句中用的是**引申义/固定搭配**
（如 `甘く見る` 教 `甘い`），那是「目标词不突出」，判 `SWAP`。

### 2. 给出重判结果与交叉表

- 新判定：`LAND` / `SWAP` 各多少条
- **与外部审核 LV 判定的交叉表**：67 条里有多少从 `LV` 变成了 `LAND`
- 变成 `LAND` 的，**全部列出**（anchor / 日文 / 中文 / 句长）—— 这是本轮主产出

### 3. `SWAP` 的从备选池换句

规则同 JP 22：`unknown_word_count` 最小 → `member` 最多 → 7–12 字优先 →
`jp_sentence_id` 最小；换上来的要重过已有机械判据。

- **❌ 不许 LLM 生成或改写任何日文/中文**（第七节）
- 无备选的（21 条里符合 `SWAP` 的部分）单独列清单，不落库不改句

### 4. 导出待审清单

`LAND` 组 + 换句成功的，导出到 `staging/lv-67-for-review.md`，
沿用 `staging/land-301-for-review.md` 的指令头与表格格式。

**本轮不落库** —— 负责人审完再单独立落库工单。

## 明确不做

- ❌ 不落库、不碰两个内容包、不开内容分支
- ❌ 不用 LLM 生成或改写任何内容
- ❌ 不改 ZH 38 组（独立工单）
- ❌ **不覆盖任何已有词场**（200 条里已有的 anchor 全部排除，用字段判断不要硬编码）
- ❌ 不改 `App.js`、UI、gloss 逻辑
- ❌ 不构建、不发 OTA、不推 `origin/main`

## 验收

- 异常自查（5-2）；每个数字附复算命令（5-1）
- **决策指标出数**（5-3）：重判后「该落」的条数
- 与外部审核 LV 判定的交叉表
- `LAND` 组全部列出
- `staging/lv-67-for-review.md` 已提交
- 脚本只读、可复现（跑两次逐字节相同）
- `npm test && npm run typecheck && npm run audit`，`git status --short` 干净

## 做完写哪里

`ACTIVE.md` + `CC-REPORT.md` **追加到文件末尾**（5-5：不要先读整份）。

**本轮主产出是「从 LV 翻成 LAND 的那批」。** 负责人要看的是
「原判据到底误伤了多少」。
