# YanApp · 灵感停车场

> 新想法先记录，不自动成为当前工单，也不打断 `ACTIVE.md`。

## 使用规则

每个想法回答：

- 想法是什么？
- 它解决用户的什么问题？
- 与“学 → 用 → 记住经历”有什么关系？
- 可能影响哪些数据结构？
- 是否涉及不可逆迁移或当前地基？
- 下一次评审时间？

只有以下情况可以打断当前工单：

- 当前实现会使新想法以后无法兼容；
- 涉及不可逆的数据迁移；
- 当前基础方向有证据表明错误；
- 存在用户数据或内容真实性风险。

## 已记录

### I1. 自动化来源审计流水线

- 问题：内容反复经历“生成 → 发现错误 → 再找源 → 再修改”；
- 方向：claim 拆分、来源家族、确定性校验、subagent 反例、自动裁决、staging patch；
- 状态：已进入路线图，但尚未实施；
- 与当前任务关系：先建立 publication 边界，再实施流水线。

### I2. 外部模型审核包

- 问题：豆包、Kimi、DeepSeek 没有仓库文件权限或稳定 API；
- 方向：导出 claim JSON，导入 evidence JSON；外部模型永不直接修改正式文件；
- 状态：source-audit 稳定后再做；
- 不作为第一版流水线依赖。

## 新想法模板

### I?. 标题

- 用户问题：
- 想法：
- 与主线关系：
- 数据结构影响：
- 是否打断当前工单：否
- 评审日期：

## 2026-08-24 · 检索练习（retrieval practice）—— 大部分已有，缺两条

来源：项目负责人的每日简报（引 PubMed 两篇：短时与 30/50 天保持上检索练习优于重复阅读；
理解方向与产出方向只能部分迁移）。

**已经有了的**（不用再做）：

- 间隔复习 → `srs.js` 的 Leitner 阶梯
- 先答后揭示 → 复习卡要翻面才看答案
- 即时反馈 → 拼句题（B5-2 之后会说「对了/错了」）
- 错误词短间隔重测 → 「忘了」回第一档且**当天再见**

**真正缺的两条**：

1. **双向卡**。现在词库词是 `recall`（日→读音/意思），场景句是 `produce`（中→日），
   但**同一个词没有双向**。研究说两个方向只能部分迁移 —— 意味着「认得」不等于「说得出」。
2. **「第一次独立想起」的比例**。现在只记评分，不记「有没有靠自己想起来」。
   这个数比「学过多少词」更能说明学会没有。

**不做的理由**：主线是词库深度（词场 20/563、深卡 8 张）。
这两条都要动 `units.js` / `srs.js` —— 那是当前不变量里明令只读的。
等主线跑到一个可交付的点再谈。

## 2026-08-24 · 评分改成滑动手势

项目负责人提议：忘了/一般/会了 改成左滑/右滑/上滑，降低操作成本。

**我的判断：想法合理，但现在不该做；真要做也是「加」不是「换」。**

- SRS 评分是**低频精确操作**，不是高频粗操作。滑动适合后者（刷信息流），
  精确三选一用按钮更稳
- 三个按钮**自解释**，三个方向不是。而第一次真人测试暴露的核心问题恰恰是
  **可发现性**（她没注意到口袋）—— 再加一层不可见的交互会让这个问题更重
- 渐进增强是安全的：**保留按钮，滑动作为快捷方式**。熟练用户自己会发现，新用户不受影响

**前置**：这属于词卡 UI 打磨，而连续三批都在打磨词卡了（见 `AGENTS.md` 第〇节）。
等主线推进之后再排。
