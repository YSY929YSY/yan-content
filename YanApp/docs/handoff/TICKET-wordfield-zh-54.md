# 工单 · ZH 54 条中文（判读在外，落库在内）

> 先读 `AGENTS.md`（第二节分支规矩、第五节 5-1~5-5、**第七节诚实要求**）。
> **前置：`TICKET-gloss-single-kana.md` 与 `TICKET-wordfield-render-fixes.md` 先做完。**

## 本轮决策指标（5-4）

**A 类 26 条改完并经负责人确认的条数：0 → ?**

B/C/D 是附带，不计入决策指标 —— **A 会教错，B/C/D 只是难看。**

不是"改了多少条"，是**改完之后负责人确认可落的条数**。

## 这张工单把主线推迟多久

**不推迟。** 词场 249 → 最多 303 / 563，是同一条主线的收尾。

## 这批是什么

`staging/zh-54-for-review.md`，54 条，三个来源：

| 来源 | 条数 | 说明 |
|---|---:|---|
| 301 轮外部审核判 `ZH` | 38 | 日语通过，中文有问题 |
| LV 轮外部复核判 `ZH` | 15 | 原先被 `LV` 标签遮住，中文从没被审过 |
| `n5_kata` 换句 | 1 | `あの方は八十歳です。/ 那位老人八十岁。`「老人」是原句没有的增补 |

**这批的日语已经审过并通过，本轮不要碰日语。**

## 🔴 最关键的约束

**中文修改**是内容，不是派生数据。`AGENTS.md` 第七节：
**LLM 产出只能是 candidate，不能直接进 `publication`；两个模型一致不是两个来源。**

所以流程必须是三步，**不许合并**：

```
1. 外部审核判读        →  只输出 A/B/C/D 分类，不写替换中文
2. 逐条给出候选中文    →  可以由 LLM 产出，但标记为 candidate
3. 负责人逐条确认      →  确认过的才允许落库
```

**Codex 在第 2 步之前不要动手。** 第 1 步的结果由负责人带回来。

## 分步

### 第 0 步（已完成，不用重做）

`staging/zh-54-for-review.md` 已生成，判据分四类：

- `A` 意思错（主语/语气/称谓/义项）
- `B` 意思对但不像人话
- `C` 语体对不上
- `D` 标点或错别字

**`D` 类可以机械修**（半角 `!?.,` → 全角；「变的/变得」这类），
其余三类必须走人工确认。

### 第 1 步 · 判读结果（已完成，2026-08-30）

```
A 意思错     26 条   2 4 6 8 10 11 13 15 17 18 19 22 23 24 27 28 31 32 34 38 41 45 46 47 53 54
B 不像人话   17 条   5 7 9 14 20 21 29 30 35 40 42 43 44 48 49 50 51
C 语体不对    5 条   3 12 25 36 52
D 标点/错别字 2 条   26 39
OK            4 条   1 16 33 37
```

**负责人明确翻回 OK 的四条，理由要记住，不要再判它们错**：

| # | 句 | 为什么是 OK |
|---|---|---|
| 1 | `大好きな色` → 「我最喜欢红色」 | 中文可自然落成「最喜欢的颜色」，不值得为机械逐字对应判错 |
| 16 | 「已将近十点了」 | 略书面但仍是正常中文，不足以判 B |
| 33 | `後悔先に立たず` → 「覆水难收」 | 谚语式意译，核心语义足够接近，不要求逐字等值 |
| 37 | `私は手をたたいた` → 「我拍手」 | **中文不强制标过去时，不能因为没有「了」就判错译** |

**这四条确立的原则：判据是「中文读者会不会理解错」，不是「有没有逐字对应」。**

### 第 2 步 · 分类处理（**按 A → D → B → C 的顺序做**）

**优先级依据（负责人定的，不要改）**：
**A 是语义漂移，会直接教错；B/C/D 是呈现质量。** A 必须先修完。

- **`A` 26 条（最优先）**：逐条给出候选中文，附一句「原中文错在哪」。
  典型形状已在判读里点明：无根据增补（「你的」「我的」「谢谢」「给我」「以前」）、
  漏译（「我的问题」「葡萄酒和少量」）、义项错（`文章`→一句话、`上着`→大衣、
  `お腹痛`→胃痛、`木に`→树里、`方`→老人）、主语/语态错（`無くなる`→主动弄丢、
  `風邪をうつした` 主语变成感冒）、范围错（`兄弟` 被缩窄为男性）
- **`D` 2 条**：机械修（#26 「甚么」字形、#39「变的」→「变得」）。
  **只改字形和标点，不动措辞。**
- **`B` 17 条 / `C` 5 条**：同样给候选，但**标明它们是呈现问题，不是错译** ——
  负责人可以选择先落库、以后再打磨。
- 任何一条判不准的，写「判不准，原因是 X」，**不要硬给**。

**全部产出到 `staging/zh-54-candidates.md`，不写进内容包。**

### 第 3 步 · 落库（负责人确认之后，另开内容分支）

**本工单不包含第 3 步。** 负责人确认完，另立落库工单。

## 明确不做

- ❌ **不改日语**（这批日语已通过审核）
- ❌ **不把任何 LLM 产出的中文直接写进内容包**
- ❌ 不落库、不碰两个内容包、不发布、不推 OTA
- ❌ 不改 gloss / 对齐 / UI（另有工单）
- ❌ 不替负责人做判读

## 验收

- 异常自查（5-2）；数字附复算命令（5-1）
- `D` 类修改前后对照表
- `staging/zh-54-candidates.md` 已提交，每条含：原中文 / 候选中文 / 错在哪
- **判不准的逐条列出**
- 决策指标：**待负责人确认的条数**（第 3 步之前不产生"可落"数字）
- `npm test && npm run typecheck && npm run audit`，`git status --short` 干净

## 做完写哪里

`ACTIVE.md` + `CC-REPORT.md`（**直接追加末尾** —— 5-5）。
