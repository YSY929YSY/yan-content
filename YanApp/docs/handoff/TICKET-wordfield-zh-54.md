# 工单 · ZH 54 条中文（判读在外，落库在内）

> 先读 `AGENTS.md`（第二节分支规矩、第五节 5-1~5-5、**第七节诚实要求**）。
> **前置：`TICKET-gloss-single-kana.md` 与 `TICKET-wordfield-render-fixes.md` 先做完。**

## 本轮决策指标（5-4）

**改完中文后可落库的条数：0 → ?**（上限 54）

不是"改了多少条"，是**改完之后负责人确认可落的条数**。

## 这张工单把主线推迟多久

**不推迟。** 词场 249 → 最多 303 / 563，是同一条主线的收尾。

## 这批是什么

`staging/zh-54-for-review.md`，54 条，三个来源：

| 来源 | 条数 | 说明 |
|---|---:|---|
| 301 轮外部审核判 `ZH` | 38 | 日语通过，中文有问题 |
| LV 轮外部复核判 `ZH` | 15 | 原先被 `LV` 标签遮住，中文从没被审过 |
| `n5_kata` 换句 | 1 | `あの方は八十歳です。/ 那位老人八十岁。`「老人」是原句没有的增补 |

**这批的日语已经审过并通过，本轮不要碰日语。**

## 🔴 最关键的约束

**中文修改**是内容，不是派生数据。`AGENTS.md` 第七节：
**LLM 产出只能是 candidate，不能直接进 `publication`；两个模型一致不是两个来源。**

所以流程必须是三步，**不许合并**：

```
1. 外部审核判读        →  只输出 A/B/C/D 分类，不写替换中文
2. 逐条给出候选中文    →  可以由 LLM 产出，但标记为 candidate
3. 负责人逐条确认      →  确认过的才允许落库
```

**Codex 在第 2 步之前不要动手。** 第 1 步的结果由负责人带回来。

## 分步

### 第 0 步（已完成，不用重做）

`staging/zh-54-for-review.md` 已生成，判据分四类：

- `A` 意思错（主语/语气/称谓/义项）
- `B` 意思对但不像人话
- `C` 语体对不上
- `D` 标点或错别字

**`D` 类可以机械修**（半角 `!?.,` → 全角；「变的/变得」这类），
其余三类必须走人工确认。

### 第 1 步 · 等负责人带回判读结果

拿到编号 + 分类后再往下。**不要自己判读代替他。**

### 第 2 步 · 分类处理

- **`D` 类**：机械修，给出修改前后对照。**只改标点和明确错别字，不动措辞。**
- **`A`/`B`/`C` 类**：逐条给出**候选中文**，一条一个，附一句「原中文错在哪」。
  **产出到 `staging/zh-54-candidates.md`，不写进内容包。**
- 任何一条判不准的，写「判不准，原因是 X」，**不要硬给**。

### 第 3 步 · 落库（负责人确认之后，另开内容分支）

**本工单不包含第 3 步。** 负责人确认完，另立落库工单。

## 明确不做

- ❌ **不改日语**（这批日语已通过审核）
- ❌ **不把任何 LLM 产出的中文直接写进内容包**
- ❌ 不落库、不碰两个内容包、不发布、不推 OTA
- ❌ 不改 gloss / 对齐 / UI（另有工单）
- ❌ 不替负责人做判读

## 验收

- 异常自查（5-2）；数字附复算命令（5-1）
- `D` 类修改前后对照表
- `staging/zh-54-candidates.md` 已提交，每条含：原中文 / 候选中文 / 错在哪
- **判不准的逐条列出**
- 决策指标：**待负责人确认的条数**（第 3 步之前不产生"可落"数字）
- `npm test && npm run typecheck && npm run audit`，`git status --short` 干净

## 做完写哪里

`ACTIVE.md` + `CC-REPORT.md`（**直接追加末尾** —— 5-5）。
