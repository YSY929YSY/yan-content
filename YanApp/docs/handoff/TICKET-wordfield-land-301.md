# 工单 · LAND 301 条落库（内容窗口，开分支）

> 先读 `AGENTS.md`（第二节文件锁与分支规矩、第四节 commit 完成标准、第五节报告规矩）。
> **这是本项目两个月来第一次真正推进主线。**
> **⚠️ 本轮改内容包 —— 必须开内容分支，必须两份文件同一个 commit。**

## 本轮决策指标（5-4）

**词场落库数：20 / 563 → 321 / 563。**

其余数字（覆盖率、状态分布）本轮都不是决策指标。

## 这张工单把主线推迟多久

**它就是主线。** 前四轮全是为它做的准备。

## 负责人已经做出的决定（不要重新讨论）

已知并接受：**301 条里约 8 条中文意思错**（人工判读 1/40 = 2.5%）。

- 接受的理由：词场 20→321 的产品价值远大于 8 条错译；纠错入口会接住它们
- **另外约 10% 是「意思对但不像人话」** —— 日语有源、不动，中文以后再打磨，
  **不是本轮的事，不要顺手改**
- **不要因为"质量还没确认"就停下来问** —— 这个问题已经答过了

## 要做的

### 1. 开内容分支

```
content/2026-08-26-wordfield-land-301
```

窗口内串行，不许并行任何其他内容改动（第三节）。

### 2. 落 301 条 LAND

从 `staging/wordfield-shortlist-343.json` 取 `review_status === "LAND"` 的 301 条，
写进内容包的词场结构。

- **按现有 20 条词场的结构写**，先读它们长什么样，**不要新造字段**
- 每条保留 Tatoeba 日句 ID 与中文句 ID（**可回读、可定位，这是 moat 的前提**）
- **日文和中文都逐字照搬，一个字都不改**
- **不落** SWAP 6 / FIX_ZH 7 / DATA 17 / SPOKEN 12 这 42 条

### 3. 标注「未经母语者确认」

301 条必须带上和现有 20 条同样的未确认标记（`AGENTS.md` 挂账第 5 条）。

**用户可见文案里不许出现内部状态词**（不变量 7）—— 这是内部字段，不是给用户看的。

### 4. 两份文件，同一个 commit

```
YanApp/assets/content.fallback.json
yan-content/content.v2.json
```

- 提交前 `shasum -a 256` 确认两份逐字节相同
- `_meta.version` 递增（**整个窗口只递增一次**）
- `bash tools/check-content-release.sh` Blocker=0
- **必须附前后 `node scripts/content-stats.mjs` 对比**（第五节）

### 5. 不变量自查（逐条在报告里回答）

- **不变量 1**：进度键格式没变，`unitKey('word', …)` 仍是裸的 `词-读音`
- **不变量 2**：`kanji_anchor` 仍是 **563**，没给任何词加这个 feature
- **不变量 3**：没碰 `srs.js`
- **不变量 5**：`Recipe`/词条的 `updatedAt` 语义不适用于言，但**词场是子对象**，
  确认它的改动会被同步链正确带上
- **不变量 7**：新增文案没有内部状态词
- **不变量 8**：没有「高频」「已核验」一类无源断言

## 明确不做

- ❌ **不发布、不推 origin/main** —— 落库和发布是两件事。
  `push-content.sh` 由负责人决定什么时候跑（**merge 到 main = 推线上**）
- ❌ 不构建、不发 OTA
- ❌ 不改任何一条句子的日文或中文
- ❌ 不落那 42 条非 LAND
- ❌ 不改 `App.js`、UI、gloss
- ❌ 不用 LLM 生成或改写任何内容（第七节）

## 验收

- `npm test && npm run typecheck && npm run audit`，贴原始输出
- `bash tools/check-content-release.sh` Blocker=0
- 两份内容包 sha256 相同（贴出来）
- `content-stats` 前后对比：**词场 20 → 321**
- 不变量自查逐条回答
- 异常自查（5-2）+ 每个数字附复算命令（5-1）
- `git status --short` 干净

## 做完写哪里

`ACTIVE.md` + `CC-REPORT.md`。

**做完后停下，不要发布。** 下一步是负责人在真机上看这 301 条长什么样。
