# 未跟踪工作区清单

本清单只记录本工单发现的本地文件；本轮没有删除或 `git add` 任何一项。权威内容副本仍是 `yan-content/content.v2.json`，下列 `content.json` / `content.v1.json` 均不应被发布脚本选中。

| 路径 | 是什么 / 产生时间 | 现在还有没有用 | 建议 |
|---|---|---|---|
| `YanApp_backup_0501/`（280M；2026-04-05 创建，2026-08-21 修改） | YanApp 本地备份目录 | 可能用于回滚或取回旧文件；本轮没有逐文件复核 | 保留，待负责人确认后再决定是否删除 |
| `resources/`（97M；2026-05-01 创建并修改） | 仓库外的本地参考资料目录 | 判断不了，需要负责人确认是否仍被研究流程使用 | 判断不了，需要负责人确认 |
| `YanApp/调研/`（796K；2026-08-21 创建，2026-08-22 修改） | 调研文档、评估包和压缩归档 | 仍可能是审计依据，但不是 App 运行时输入 | 保留，待负责人确认归档策略 |
| `yan-content/content.json`（316K；2026-04-29 创建，2026-06-08 修改） | yan-content 的旧内容包副本 | **高风险：与权威 `content.v2.json` 同目录，形状像可发布包；本轮 grep 未发现脚本读取** | 建议负责人确认后删除；在确认前保留但必须忽略 |
| `yan-content/content.v1.json`（316K；2026-05-20 创建） | yan-content 的 v1 内容包副本 | **高风险：不是权威副本，和 `content.v2.json` 并列容易误发；本轮 grep 未发现脚本读取** | 建议负责人确认后删除；在确认前保留但必须忽略 |
| `yan-content/README.md`（4K；2026-04-29 创建） | yan-content 本地克隆说明 | 可能说明旧内容仓库用途；是否仍需要无法从代码判断 | 保留，待负责人确认 |
| `yan-content/yan_word_story输入模式探讨26.6.2.html`（16K；2026-06-02 创建） | 词条故事输入模式的设计讨论 HTML | 研究参考，不是运行时输入；是否仍需归档无法判断 | 判断不了，需要负责人确认 |

## 引用检查

按工单要求检查：

```text
grep -rn "content\.v1\.json\|yan-content/content\.json" --include=*.mjs --include=*.js --include=*.sh --include=*.py . ..
(无输出)
```

本轮只增加根级 `.gitignore` 规则以覆盖这些本地路径；没有删除文件，也没有把它们加入 Git。根级忽略规则同时屏蔽已存在的本地构建缓存、根级 staging 与 Tatoeba 数据目录，原因是它们同样不是仓库交付物。
