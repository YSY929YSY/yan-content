# 当前状态 · PLAN v2 第七批

> 状态：B7-1 至 B7-4 已完成；本批纯代码，内容包未修改。
>
> 更新日期：2026-08-24

## 本轮完成

- B7-1：删除口袋按钮下的解释文案，保留“已存本机，联网后同步”。
- B7-2：词详情页词性标签改为内容自适应的灰底，不改词书列表标签。
- B7-3：词场标签改为“这句话里，它和这些词碰面”，成员词在句中高亮；成员查不到时不渲染高亮。
- B7-4：新增运行时派生的词场逐词中文对齐。20 条句子共 133 个 token，115 个有中文，18 个留空，覆盖率 86.5%；留空最多且本样板实际唯一的类别是活用碎片（18 个）。

## 提交与验收

- `2a798ab`：B7 四处词卡修补与逐词中文派生函数/测试。
- `npm test`：596 passed；`npm run typecheck`：通过。
- `npm run audit`：exit 0，FAIL 0，WARN 7。
- 内容包两份文件未修改，版本仍为 2.4；B6-3 主 CTA 的改动随本批代码保留。

## 明确未做

没有改内容包、`units.js`、`srs.js`、`publication.ts`、内容管线、Sudachi 辞书形产出、LLM 中文、N3/N2/N1、词源、深卡扩展、口袋数据结构、语料频率、英日优先、地铁游戏化、手账、地图性能、内容缺口或线上发布。

## 诚实说明

B7-4 的逐词中文只来自词库直查、reading 直查和固定语法表；查不到的活用碎片留空，没有使用 LLM。语序正常的中文继续使用内容包现有句子，不重写。
