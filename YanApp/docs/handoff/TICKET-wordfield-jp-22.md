# 工单 · JP 22 条换句（从备选池换，不写新句）

> 先读 `AGENTS.md`（第二节分支规矩、第五节 5-1/5-2/5-3/5-4、第七节诚实要求）。
> **前置：`TICKET-wordfield-land-174.md` 必须先落完并合回 `develop/v2`。**
> 两张都改内容包，**内容窗口互斥，不许并行**（第三节）。

## 本轮决策指标（5-4）

**换句后仍需人工换掉的条数。**

不是"换了多少条"（那是工作量），是"**换完还剩几条不行**"——
那才是决定这批能不能收尾的数。

## 这张工单把主线推迟多久

**不推迟。** 词场 187 → 最多 209 / 563，是同一条主线的续做。

## 背景

外部审核把 301 条 LAND 里的 22 条判为 `JP`（日语不自然 / 翻译腔 / 语义不得体），
清单在 `staging/gpt-verdicts-301.json` 的 `JP` 组。典型形状：

| anchor | 句子 | 判读 |
|---|---|---|
| `n5_atama` | 彼は私の頭をぶった。 | 用「打头」教「头」，语义不得体 |
| `n5_han` | 郵便局は半マイル向こうにある。 | 英美度量衡，翻译腔 |
| `n5_ane` | 姉はタイプが上手です。 | `タイプが上手` 现代日语别扭 |

**这 22 条与现有 20 条词场零重叠**（已核），不会撞上落库那轮的 7 条问题。

## 关键：不写新句，从备选池换

```
备选池 1508 条；这 22 条里 16 条有备选，6 条是独苗
复算：node -e 'const v=require("./staging/gpt-verdicts-301.json");console.log(v.JP.length, v.JP.filter(x=>x.alt===0).length)'
```

**❌ 绝对不许用 LLM 生成或改写日语。**（第七节：LLM 产出只能是 candidate，
两个模型一致不是两个来源。）言的 moat 是每条句子可定位到 Tatoeba 原始 ID，
写一句进去就把这条断了。

## 要做的

### 1. 换句（16 条有备选的）

对每个有备选的 anchor，按**原选句规则**从剩余候选里取次优：
`unknown_word_count` 最小 → `member_word_ids` 最多 → 7–12 字优先 → `jp_sentence_id` 最小。

**换上来的句子必须重新过一遍已有的机械判据**（负面动词、外国度量衡、
中文信号、难度信号），命中就继续往下取。全部候选都命中 → 归入"无可用备选"。

### 2. 新句子仍要人判

换上来的 16 条**不能自动认为合格** —— 机械判据只能排除已知形状。

把换后的句子按外部审核那份格式导出到
`staging/jp-22-swapped-for-review.md`（沿用 `staging/land-301-for-review.md` 的
指令头和表格格式），交给负责人走同一条外部审核流程。

### 3. 独苗 6 条 + 无可用备选的，单独列清单

给出 anchor / 现有句 / 为什么不行 / 备选池为什么用不了。
**这批不落库，也不改句子** —— 它们是唯一可能需要人写日语的部分，由负责人决定。

### 4. 顺带补管线的去重（本轮做）

`scripts/wordfield-shortlist.mjs` 从来没检查过「这个 anchor 是不是已经有词场」。
343 条里 11 条与现有 20 条重叠，落库那轮撞上了 7 条。

加一道过滤：**候选来源里排除 `wordBank` 中已有非空 `wordField` 的 anchor**。
用字段判断，不要硬编码 id。给出过滤掉多少条。

## 明确不做

- ❌ **不用 LLM 生成或改写任何日文或中文**
- ❌ 不改 ZH 38 / LV 67 两组（各自有工单）
- ❌ **不覆盖现有 20 条词场的任何一条**
- ❌ 不发布、不推 `origin/main`、不构建、不发 OTA
- ❌ **本轮不落库** —— 换句结果先给负责人审，审完再单独立落库工单

## 验收

- 异常自查（5-2）；每个数字附复算命令（5-1）
- **决策指标出数**：换完仍不行的条数（独苗 6 + 无可用备选的）
- 16 条换句前后对照表：anchor / 旧句 / 新句 / 新句的 Tatoeba ID / 淘汰了几个候选
- `staging/jp-22-swapped-for-review.md` 已提交
- 管线去重过滤掉的条数
- `npm test && npm run typecheck && npm run audit`，`git status --short` 干净

## 做完写哪里

`ACTIVE.md` + `CC-REPORT.md` 追加新一节。

**本轮不改内容包，因此不需要开内容分支。**
